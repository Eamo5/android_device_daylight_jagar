# Firmware images for Daylight DC-1 (jagar)
#
# Conservative baseline: do not ship firmware partitions in the ROM until the
# OTA update policy is decided. Stock extracted images currently include:
# dpm.img, gz.img, lk.img, mcupm.img, pi_img.img, scp.img, spmfw.img, sspm.img,
# tee.img, dtbo.img, vbmeta*.img, boot.img, and vendor_boot.img.
